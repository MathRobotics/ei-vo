[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ei_vo"
version = "0.0.1"
description = "Trajectory rendering and kinematics utilities"

license = {text = "MIT License"}
requires-python = ">=3.10"
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
]
dependencies = [
    "numpy>=2.0.2",
    "matplotlib>=3.10.8",
    "mujoco>=3.4.0",
]

[project.optional-dependencies]
all = [
    "meshcat>=0.3.2",
    "pin>=3.7.0",
    "pyrender>=0.1.45,<0.2",
    "urdfpy>=0.0.22,<0.1",
    "literobo",
]
kinematics = [
    "pin>=3.7.0",
    "literobo",
]
literobo = [
    "literobo",
]
meshcat = [
    "meshcat>=0.3.2",
    "pin>=3.7.0",
]
pinocchio = [
    "pin>=3.7.0",
]
pyrender = [
    "pyrender>=0.1.45,<0.2",
    "urdfpy>=0.0.22,<0.1",
]
plot = [
]
recording = [
]
dev = [
    "meshcat>=0.3.2",
    "pin>=3.7.0",
    "pyrender>=0.1.45,<0.2",
    "pytest",
    "urdfpy>=0.0.22,<0.1",
    "literobo",
]

[project.scripts]
ei-vo-play = "ei_vo.cli.playback:main"
ei-vo-demo = "ei_vo.cli.demo:main"
